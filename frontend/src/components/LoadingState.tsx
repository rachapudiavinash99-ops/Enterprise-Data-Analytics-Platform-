import React from 'react';

const LoadingState = (props: any) => {
  return (
    <div className='loadingstate-component'>
      LoadingState Component
    </div>
  );
};

export default LoadingState;
