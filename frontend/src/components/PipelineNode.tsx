import React from 'react';

const PipelineNode = (props: any) => {
  return (
    <div className='pipelinenode-component'>
      PipelineNode Component
    </div>
  );
};

export default PipelineNode;
